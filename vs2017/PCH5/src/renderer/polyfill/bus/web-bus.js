export default class WebBus {
  constructor() {
  }

  send(event, param) {
  }

  on(event, eventHandler) {
  }

  once(event, eventHandler) {
  }

  removeListener(event, eventHandler) {
  }

  removeAllListeners(events) {
  }
}